

export function initSimulatorEvent() {
  console.log('이벤트 등록 시작')
  // 이벤트
  const tenPerBtn = document.getElementById('scroll-select-10');
  const sixtyPerBtn = document.getElementById('scroll-select-60');
  const hundredPerBtn = document.getElementById('scroll-select-00');
  const resetBtn = document.getElementById('reset-button');
  const purchaseBtn = document.getElementById('purchase-reset-button');

  console.log(tenPerBtn);


  // tenPerBtn.addEventListener('mouseup', (e) => {
  //   console.log('10퍼센트 BLUR');
  //   tenPerBtn.blur();
  // })
  // sixtyPerBtn.addEventListener('mouseup', (e) => sixtyPerBtn.blur())
  // hundredPerBtn.addEventListener('mouseup', (e) => hundredPerBtn.blur())
  // resetBtn.addEventListener('mouseup', (e) => resetBtn.blur())
  // purchaseBtn.addEventListener('mouseup', (e) => purchaseBtn.blur())
}


